"""Resource classes for the SDK client.

Each resource exposes a small, focused API mirroring the HTTP endpoints.
Resources are attached to a client instance (``client.jobs``, ``client.brands``,
etc.) rather than instantiated directly.
"""
